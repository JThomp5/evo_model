#ifndef RPI_VERTEX
#define RPI_VERTEX

#include "Vertex.h"
#include <set>

using namespace std;

template < typename T >
class Network{
 public:
  Network();
  ~Network();

 private:
  set < Vertex < T > > V;
}

#endif
